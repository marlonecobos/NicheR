% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ellipsoid.R
\name{add_data}
\alias{add_data}
\title{Add occurrence points oor other data to an existing E-space plot}
\usage{
add_data(
  data,
  x,
  y,
  pts_col = "#000000",
  pts_alpha = 1,
  col_layer = NULL,
  pal = heat.colors(100),
  rev_pal = FALSE,
  pch = 1,
  bg_sample = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame containing the points to plot. Must include
columns matching \code{x} and \code{y}, and \code{col_layer} if provided.}

\item{x}{Character. Name of the column to use as the x-axis variable.}

\item{y}{Character. Name of the column to use as the y-axis variable.}

\item{pts_col}{Character. Color for all points when \code{col_layer} is
\code{NULL}. Default is \code{"#000000"} (black).}

\item{pts_alpha}{Numeric in \code{[0, 1]}. Transparency of points when
\code{col_layer} is \code{NULL}. Default is \code{1} (fully opaque).}

\item{col_layer}{Character or \code{NULL}. Name of a column in \code{data}
to use for coloring points by a continuous variable. If \code{NULL}
(default), all points are drawn with \code{pts_col}.}

\item{pal}{A color palette function or character vector of colors used when
\code{col_layer} is provided. Default is \code{heat.colors(100)}.}

\item{rev_pal}{Logical. If \code{TRUE}, reverses the color palette before
applying it. Default is \code{FALSE}.}

\item{pch}{Integer or character. Point symbol. Default is \code{1}.}

\item{bg_sample}{Integer or \code{NULL}. If provided and \code{nrow(data)}
exceeds this value, a random subsample of this size is drawn before
plotting. Useful for large data frames. Default is \code{NULL} (plot all).}

\item{...}{Additional arguments passed to \code{\link[graphics]{points}}.}
}
\value{
Called for its side effect of adding points to the current plot.
  Returns \code{NULL} invisibly.
}
\description{
Adds points to an existing environmental space plot created with
\code{plot_ellipsoid()}. Points can be plotted with a single color or
colored by a continuous variable (e.g., suitability) using a color palette.
}
\details{
When \code{col_layer} is provided, points are colored by the log-transformed
values of that column mapped onto the palette. Zeros and \code{NA}s in
\code{col_layer} are removed before plotting.
}
\seealso{
\code{\link{plot_ellipsoid}}, \code{\link{add_ellipsoid}}
}
