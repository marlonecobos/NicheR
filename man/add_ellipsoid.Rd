% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ellipsoid.R
\name{add_ellipsoid}
\alias{add_ellipsoid}
\title{Add an ellipsoid boundary to an existing E-space plot}
\usage{
add_ellipsoid(
  object,
  dim = c(1, 2),
  lty = 1,
  lwd = 1,
  col_ell = "#000000",
  pch = 1,
  alpha_ell = 1,
  cex_ell = 1,
  ...
)
}
\arguments{
\item{object}{A \code{nicheR_ellipsoid} object.}

\item{dim}{Integer vector of length 2. Indices of the two dimensions to
plot. Default is \code{c(1, 2)}.}

\item{lty}{Integer. Line type. Default is \code{1} (solid).}

\item{lwd}{Numeric. Line width. Default is \code{1}.}

\item{col_ell}{Character. Color of the ellipsoid boundary line. Default is
\code{"#000000"} (black).}

\item{pch}{Integer or character. Point symbol (passed through but not used
by the line). Default is \code{1}.}

\item{alpha_ell}{Numeric in \code{[0, 1]}. Transparency of the ellipsoid
boundary line. Default is \code{1} (fully opaque).}

\item{cex_ell}{Numeric. Size scaling for the ellipsoid boundary. Default
is \code{1}.}

\item{...}{Additional arguments passed to \code{\link[graphics]{lines}}.}
}
\value{
Called for its side effect of adding lines to the current plot.
  Returns \code{NULL} invisibly.
}
\description{
Draws the 2D boundary of a \code{nicheR_ellipsoid} object onto an existing
environmental space plot created with \code{plot_ellipsoid()}. The boundary
is computed as a cross-section of the ellipsoid at the chosen pair of
dimensions.
}
\seealso{
\code{\link{plot_ellipsoid}}, \code{\link{add_data}}
}
